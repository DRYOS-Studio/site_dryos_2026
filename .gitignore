# macOS
.DS_Store

# Imagens originais (preservadas localmente — não versionadas pra manter o repo leve)
images/**/originals/

# Editor / OS
*.swp
.idea/
.vscode/
Thumbs.db
.vercel
# Worktrees e estado local de agente (nunca versionar — já entraram como gitlink por acidente)
.claude/worktrees/
